# Optional: raises the GitHub API rate limit for build-time repository metrics.
GITHUB_TOKEN=

# Optional: refreshes the exact Google Scholar profile totals through SerpAPI.
SERPAPI_KEY=

# Optional fallback: improves reliability for verified paper-level metrics.
SEMANTIC_SCHOLAR_API_KEY=
